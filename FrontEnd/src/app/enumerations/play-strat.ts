export enum PlayStrat {
    Stop="Music stop when finished",
    Loop="Music loop itself at end",
    Next="Pass to next music when ended"
}

export enum PlayAction {
    Play="play",
    Pause="pause",
    Stop="stop",
    End="end"
}